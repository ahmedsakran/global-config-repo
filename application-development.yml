security:
  jwt:
    enabled: true

logging:
  level:
    root: INFO
    com.mebroker: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info